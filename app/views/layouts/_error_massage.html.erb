<% if obj.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <%= obj.errors.count %>errors prohibited this book from being saved:
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>